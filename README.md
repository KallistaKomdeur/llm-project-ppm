# Title

# How to use

Run the main program:

```
python -m main <log_name> <provider> <model_type> <aggregation_method> <prompt>
```

log_name: whatever is in logs folder. Testing on bpic2020
provider: gemini, openai, or anthropic. For now only gemini freely accessible
model_type:
aggregation_method:
prompt: select from prompts in prompt folder
